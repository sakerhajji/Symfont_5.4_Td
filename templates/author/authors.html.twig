<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    

    <table border="1" align="center">
        <tr>
            <th>id</th>
            <th>username</th>
            <th>email</th>
            <th>edit</th>
            <th>delete</th>
        </tr>
        {% for i in auth %}
        <tr>
            <td>{{i.id}}</td>
            <td>{{i.username}}</td>
            <td>{{i.email}}</td>
             <td><a href="{{ path('app_edit', {'id': i.id}) }}">edit</a></td>
             <td><a href="{{ path('app_delete', {'id': i.id}) }}">delete</a></td>
        </tr>
        {% endfor %}
    </table>
    
</body>
</html>